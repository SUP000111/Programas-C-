// Programa 1
#include <stdio.h>
int main()
{
  float base, altura, area;
  printf("Cuando se mide la base \n");
  scanf("%f", &base);
  printf("Cuando mide la altura \n");
  scanf("%f", &altura);
  
  area=(base*altura)/2;
  printf("el area es %f", area);
}